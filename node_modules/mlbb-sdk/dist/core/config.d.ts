import type { Language } from '../types/common.js';
export interface MlbbClientConfig {
    /**
     * Base URL of the API.
     * @default 'https://openmlbb.fastapicloud.dev/api'
     */
    baseUrl?: string;
    /**
     * Default language for all requests. Can be overridden per-request.
     * @default 'en'
     */
    lang?: Language;
    /**
     * Default number of results per page. Can be overridden per-request.
     * @default 20
     */
    defaultPageSize?: number;
    /**
     * Request timeout in milliseconds.
     * @default 10000
     */
    timeout?: number;
    /**
     * Number of automatic retries on failure.
     * @default 1
     */
    retries?: number;
    /**
     * Additional HTTP headers sent with every request.
     */
    headers?: Record<string, string>;
}
export interface ResolvedConfig {
    baseUrl: string;
    lang: Language;
    defaultPageSize: number;
    timeout: number;
    retries: number;
    headers: Record<string, string>;
}
export declare function resolveConfig(config: MlbbClientConfig): ResolvedConfig;
//# sourceMappingURL=config.d.ts.map