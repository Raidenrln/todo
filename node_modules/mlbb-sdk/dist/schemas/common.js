import { z } from 'zod';
export const LanguageSchema = z.enum([
    'en', 'id', 'ru', 'es', 'pt', 'tr',
    'ar', 'de', 'fr', 'it', 'ja', 'ko',
    'th', 'vi', 'zh-CN', 'zh-TW',
]);
export const SortOrderSchema = z.enum(['asc', 'desc']);
export const RankSchema = z.enum(['all', 'epic', 'legend', 'mythic', 'honor', 'glory']);
export const DaysSchema = z.union([
    z.literal(1), z.literal(3), z.literal(7), z.literal(15), z.literal(30),
]);
export const PastDaysSchema = z.union([
    z.literal(7), z.literal(15), z.literal(30),
]);
export const HeroRoleSchema = z.enum(['tank', 'fighter', 'assassin', 'mage', 'marksman', 'support']);
export const HeroLaneSchema = z.enum(['exp', 'mid', 'roam', 'jungle', 'gold']);
export const SortFieldSchema = z.enum(['pick_rate', 'ban_rate', 'win_rate']);
//# sourceMappingURL=common.js.map