import { HttpClient } from './core/http.js';
import { resolveConfig } from './core/config.js';
import { MlbbModule } from './modules/mlbb.js';
import { AcademyModule } from './modules/academy.js';
import { UserModule } from './modules/user.js';
import { AddonModule } from './modules/addon.js';
export { UserSession } from './modules/user.js';
/**
 * Create an MLBB API client.
 *
 * @example
 * ```ts
 * const client = createMlbbClient({ lang: 'ru', defaultPageSize: 10 })
 *
 * const heroes = await client.mlbb.getHeroes()
 * const ranks  = await client.mlbb.getHeroRanks({ rank: 'mythic', days: 7 })
 * const items  = await client.academy.getEquipmentExpanded()
 * ```
 */
export function createMlbbClient(config = {}) {
    const resolved = resolveConfig(config);
    const http = new HttpClient(resolved);
    return {
        mlbb: new MlbbModule(http),
        academy: new AcademyModule(http),
        user: new UserModule(http),
        addon: new AddonModule(http),
    };
}
//# sourceMappingURL=index.js.map