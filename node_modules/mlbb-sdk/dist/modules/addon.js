// ─── AddonModule ──────────────────────────────────────────────────────────────
export class AddonModule {
    http;
    constructor(http) {
        this.http = http;
    }
    /**
     * Calculate consecutive wins needed to reach a target win rate.
     *
     * @example
     * ```ts
     * const result = await client.addon.calculateWinRate({
     *   matchNow: 100,
     *   wrNow: 50,
     *   wrFuture: 75,
     * })
     * console.log(result.required_no_lose_matches) // 100
     * ```
     */
    calculateWinRate(params) {
        return this.http.get('/addon/win-rate-calculator', {
            'match-now': params.matchNow,
            'wr-now': params.wrNow,
            'wr-future': params.wrFuture,
        });
    }
    /**
     * Retrieve geographic location details for the caller's IP address.
     *
     * @example
     * ```ts
     * const location = await client.addon.getIpLocation()
     * console.log(location.data?.country) // 'id'
     * ```
     */
    getIpLocation() {
        return this.http.get('/addon/ip');
    }
}
//# sourceMappingURL=addon.js.map