import type { HttpClient } from '../core/http.js';
import type { AddonWinRateResponse, AddonIpResponse } from '../types/addon.js';
export interface WinRateCalculatorParams {
    /** Current total number of matches played (≥ 0). */
    matchNow: number;
    /** Current win rate in percent (0–100). */
    wrNow: number;
    /** Target win rate in percent. Must be greater than `wrNow` and between 0–100. */
    wrFuture: number;
}
export declare class AddonModule {
    private readonly http;
    constructor(http: HttpClient);
    /**
     * Calculate consecutive wins needed to reach a target win rate.
     *
     * @example
     * ```ts
     * const result = await client.addon.calculateWinRate({
     *   matchNow: 100,
     *   wrNow: 50,
     *   wrFuture: 75,
     * })
     * console.log(result.required_no_lose_matches) // 100
     * ```
     */
    calculateWinRate(params: WinRateCalculatorParams): Promise<AddonWinRateResponse>;
    /**
     * Retrieve geographic location details for the caller's IP address.
     *
     * @example
     * ```ts
     * const location = await client.addon.getIpLocation()
     * console.log(location.data?.country) // 'id'
     * ```
     */
    getIpLocation(): Promise<AddonIpResponse>;
}
//# sourceMappingURL=addon.d.ts.map